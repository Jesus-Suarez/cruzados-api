el servidor inicia con

se ejecuta con: para el enviroment de development
-- npm run dev ---Es el script que esta definido en el package.json

Para produccion cambiar en el archivo .env
NODE_ENV=production
